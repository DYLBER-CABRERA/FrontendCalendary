import { Schedule } from './Schedule';

export class Classes {
  code?: string;
  professorId?: number;
  campusId?: string;
  subjectCode?: string;
  startDate?: Date;
  endDate?: Date;
  schedule?: Schedule;
}